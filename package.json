{
  "name": "rust-fetch",
  "version": "1.1.0",
  "publishConfig": {
    "access": "public"
  },
  "export": {
    "fetch": "./wasm-module/pkg/wasm_module"
  },
  "scripts": {
    "build": "cd wasm-module && tsc",
    "wasm:build": "cd wasm-module && rm -rf pkg && wasm-pack build --target web && cd pkg && rm -rf .gitignore README.md && cp ../../README.md .",
    "wasm:version": "cd wasm-module && cargo release ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "path": "^0.12.7",
    "wasm-module": "file:wasm-module/pkg"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "typescript": "^5.7.3"
  },
  "description": ""
}
